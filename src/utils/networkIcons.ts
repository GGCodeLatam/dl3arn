import Polygon from "assets/networks/matic.svg";

const networkIcons: { [key: string]: { icon: string; color: string } } = {
  polygon: { icon: `/${Polygon}`, color: "#6F41D8" },
};

export default networkIcons;
