import { query } from "firebase/firestore";
import { APIQueryCourses } from "utils/types/course";

async function searchCourse(q: string): Promise<APIQueryCourses> {
  return [];
}

export default searchCourse;
